# AI/ML packages - Install separately or on a different service
# These packages are VERY memory intensive during installation
# Only install if you have sufficient memory (8GB+ build environment)

# Vector database and embeddings
chromadb
chromadb-client
sentence-transformers

# LangChain ecosystem
langchain==1.0.8
langgraph==1.0.3
langgraph-checkpoint==3.0.1
langgraph-prebuilt==1.0.5
langgraph-sdk==0.2.9
langsmith==0.4.46
langchain-google-genai

# PDF and document processing
pypdf

# Web scraping
beautifulsoup4
lxml

# Scheduling
APScheduler

# Additional dependencies for AI features
msgpack==1.1.2
ormsgpack==1.12.0
xxhash==3.6.0
zstandard==0.25.0
tqdm==4.67.1
typing-inspection==0.4.2

# Note: Installing these packages requires:
# - At least 8GB RAM during build
# - About 2-3GB disk space
# - 10-15 minutes build time
# 
# Recommended: Deploy these on a separate service or use:
# - Render Pro plan ($25/month)
# - Railway (better ML support)
# - Modal (serverless ML)
# - Managed vector DB (Pinecone, Weaviate)